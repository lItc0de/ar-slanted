import React from 'react';
import spinnerStyles from './loading-spinner.module.scss';

const LoadingSpinner = () => {
  return <div className={spinnerStyles.loadingSpinner}></div>
}

export default LoadingSpinner;
